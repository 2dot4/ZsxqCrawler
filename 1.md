# 下载结果镜像到根目录 download 目录的修改指令

以下步骤用于让网页点击下载后，除了原有的 `output/databases` 目录外，同时在项目根目录生成一份 `download/` 镜像副本。

1. **新增/扩展下载目录工具（`db_path_manager.py`）**
   - 在文件底部增加两个方法：`get_root_download_dir()` 返回根目录下的 `download` 路径并确保存在；`mirror_file_to_root_downloads(source_path, filename=None)` 将已下载文件复制到该目录并返回目标路径。
   - 这些工具函数独立于数据库逻辑，供其他模块调用。

2. **文件下载器接入镜像（`zsxq_file_downloader.py`）**
   - 导入 `mirror_file_to_root_downloads`。
   - 在文件已存在且大小匹配时、以及新下载完成后，调用镜像函数把文件同步到根目录，并记录成功/失败日志，保证两处存储一致。

3. **后台任务的专栏下载流程（`main.py`）**
   - 在专栏文件与专栏视频下载逻辑中，完成或跳过下载时调用 `mirror_file_to_root_downloads`，将本地路径同步到根目录 `download/`，并将镜像结果写入任务日志。

4. **忽略与文档（`.gitignore` 与 `README.md`）**
   - `.gitignore` 新增 `download/` 以忽略运行时镜像目录。
   - `README.md` 补充说明：所有下载文件会在原有存储位置外，额外复制到根目录的 `download` 文件夹，便于直接取用。

按照上述文件和步骤修改，即可让所有网页触发的下载在保留原路径的同时，在根目录生成一份可直接访问的副本。
